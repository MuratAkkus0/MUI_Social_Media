import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#000000",
    },
    secondary: {
      main: "#fff",
    },
    otherColor: {
      main: "#ccc",
    },
  },
});
export default Theme;
